spring.application.name=security-monitor

# MongoDB dependency is included, but auto-configuration is disabled for now so the app
# can start cleanly without a running database in this initial prototype stage.
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration,org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration
